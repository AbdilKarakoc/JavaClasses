package com.class12;

public class NewTask {

	

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		
		// Create two Strings and initaialize them with some value.
        //implement the following methods on those strings.
        
        /*
         * sen.length();
         * sen.equals();
         * sen.contains(s);
         * sen.toUpperCase();
         * sen.toLowerCase();
         * sen.equalsIgnoreCase(anotherString);
         */
		
		String name="Abdil Esenyazi Koyu";
		String name1="Karakoc";
		System.out.println(name+" "+name1);
		System.out.println(name.length()+name1.length());
		System.out.println(name==name1);
		System.out.println(name.startsWith("A"));
		System.out.println( name.contains("Esenyazi"));
		
	}
}
